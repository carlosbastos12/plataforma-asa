# Plataforma-ASA — variáveis de ambiente
#
# Nesta fase o projeto é uma demonstração 100% front-end: sem backend,
# banco de dados, autenticação ou API real (ver CLAUDE.md, seção 6).
# Não há nenhuma variável de ambiente obrigatória hoje.
#
# Este arquivo existe como convenção de publicação e será preenchido
# quando a Fase 3 (backend real) for iniciada — ver ROADMAP.md.
